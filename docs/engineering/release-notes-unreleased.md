# Unreleased

Notes for the next release. Each entry is written when the change lands, so the
release itself is an editing job rather than an archaeology one.

Nothing yet since [v0.9.0-rc.1](release-notes-v0.9.0-rc.1.md).
